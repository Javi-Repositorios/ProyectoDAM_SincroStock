


/// TAREA CRUD ARTICULOS

// Se crea el modelo, controlador, servicio y repositorio de Articulos.
		* se crea su vez el modelo y  servicio de lineas_de_pedido por ser requerido por Spring al estar relacionado en tablas.

// Se crea el servicio de validación.

// Se añaden los archivos html, js, css al cliente.

// Restructuracion y reparacion de rutas y funcionamiento general de interfaz.
 	( Se necesito una página extra para gestionar la ambivalencia de roles ) --> SeleccionPerfil.html




/// TAREA CRUD CLIENTES

// Se crea el modelo, controlador, servicio y repositorio de Clientes.

// Se añaden los archivos html, js, css al servidor NGINX cliente.

// Servicio de validacion para editar o crear Clientes en front y back end.



///PRUEBAS

Se añade la documentacion perteneceiente a las pruebas.

Corregido lógica de validacion incoherente en el servicio de  validación  de articulos.
 ( Error: Permitia 1 de los valores negativos, ahora no permite inguno) 


Corregida lógica incoherente en la validacion de clientes.
 ( Error: No se tuvo en cuenta que Spring actualiza el registro si detecta un id existente, en vez de dar error de duplicado)
   Solucion: Se comprueba si el id existe dentro de la validacion para lanzar el error)